include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Generated by isar
    - lib/service/db/message.g.dart
    - lib/service/db/roster.g.dart
    - lib/service/db/conversation.g.dart

linter:
  rules:
    # avoid_print: false  # Uncomment to disable the `avoid_print` rule
